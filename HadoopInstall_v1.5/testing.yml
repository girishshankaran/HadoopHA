---
  - hosts: cluster01
    name: Number of hosts
    vars:
      hostcount: "{{ ansible_play_hosts | length }}"
    tasks:
#    - name: Find the number
      package:
         name: wget
         state: latest
         register: output1
      debug:
         msg: "Number is {{ hostcount }}"
  
  
